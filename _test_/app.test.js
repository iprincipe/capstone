import { generatePage } from "../src/client/js/app"

describe("Testing the generate function", () => {  
    test("Testing the generatePage() function", () => {
           expect(generatePage).toBeDefined();
})});